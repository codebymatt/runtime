// NOTE: functionality here is tested in the integrations_test folder
package db

import "runtime/api/models"

func CreateUser(u models.User) (id int, e error) {
	// Write SQL
	// Make call
	return 0, nil
}
