import styled from "styled-components";

const RunInputTitle = styled.h5`
  text-transform: uppercase;
  font-size: 16px;
  font-weight: bold;
  margin: 0px auto 6px;
  text-align: right;

  @media (max-width: 420px) {
    text-align: left;
  }
`;

export default RunInputTitle;
